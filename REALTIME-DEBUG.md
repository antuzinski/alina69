# Инструкция по отладке Realtime и оповещений

## Проблема
- Задачи не синхронизируются между устройствами автоматически
- Оповещения не приходят при изменениях на другом устройстве

## Что исправлено

### В базе данных
1. ✅ Включен Realtime для таблицы `tasks`
2. ✅ Настроен `REPLICA IDENTITY FULL` (для получения старых значений при UPDATE)
3. ✅ RLS политики разрешают всем читать и изменять задачи

### В коде
1. ✅ Добавлена Realtime подписка в TaskManager
2. ✅ Добавлено подробное логирование всех событий
3. ✅ Оповещения теперь вызываются синхронно в Realtime callback

## Шаг 1: Проверка базовых оповещений

1. Откройте **https://alina.quest/test-notifications.html**
2. Нажмите "Запросить разрешение" и разрешите оповещения
3. Нажмите "Тестовое оповещение"
4. **Должно появиться оповещение немедленно**

### Если белая страница
- Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Проверьте, что файл загружается (откройте DevTools → Network)

### Если оповещение не приходит
- Проверьте статус разрешения на странице
- iOS: используйте Safari (Chrome на iOS не поддерживает)
- Android: проверьте настройки Chrome → Сайты → Оповещения
- Проверьте, что не включен режим "Не беспокоить"

## Шаг 2: Проверка Realtime подписки

1. Откройте приложение **https://alina.quest**
2. Откройте консоль браузера (F12 → Console)
3. Найдите логи `[TaskManager]`

### Что должно быть в консоли при загрузке:
```
[TaskManager] Setting up Realtime subscription...
[TaskManager] Subscription status: SUBSCRIBED
[TaskManager] ✅ Successfully subscribed to Realtime
```

### Если статус не SUBSCRIBED
Проверьте в консоли:
- Есть ли ошибки от Supabase
- Проверьте логи `[SUPABASE] Initializing with:` - должны быть URL и ключ
- Проверьте, что вы залогинены (`[SUPABASE] Auth state change`)

## Шаг 3: Проверка синхронизации между устройствами

### Настройка
1. Откройте приложение на двух устройствах (десктоп + мобильный)
2. Войдите под одним аккаунтом на обоих
3. На обоих откройте консоль браузера (F12 → Console)

### Тест 1: Создание задачи
1. **На десктопе:** создайте новую задачу
2. **В консоли десктопа** должно быть:
   ```
   [TaskManager] 🔥 Realtime event received: INSERT
   [TaskManager] New task detected: {title: "...", ...}
   [TaskManager] Showing notification for new task
   ```
3. **В консоли мобильного** должно быть ТО ЖЕ САМОЕ (в течение 1-2 секунд)
4. **На мобильном** должно прийти оповещение "Новая задача добавлена"
5. Задача должна появиться в списке на мобильном автоматически

### Тест 2: Выполнение задачи
1. **На мобильном:** отметьте задачу как выполненную
2. **В консоли мобильного** должно быть:
   ```
   [TaskManager] 🔥 Realtime event received: UPDATE
   [TaskManager] Task updated. Old: {...}, New: {...}
   [TaskManager] Showing notification for completed task
   ```
3. **В консоли десктопа** должно быть ТО ЖЕ САМОЕ
4. **На десктопе** должно прийти оповещение "Задача выполнена"
5. Задача должна обновиться на десктопе автоматически

## Диагностика проблем

### Проблема: Realtime события не приходят

**Проверьте в консоли:**
```javascript
// На обоих устройствах выполните:
const channel = supabase.channel('test-' + Date.now())
  .on('postgres_changes', {
    event: '*',
    schema: 'public',
    table: 'tasks'
  }, (payload) => {
    console.log('TEST EVENT:', payload);
  })
  .subscribe((status) => {
    console.log('TEST SUBSCRIPTION:', status);
  });
```

Если статус не `SUBSCRIBED`:
- Проверьте интернет соединение
- Проверьте, что Supabase проект активен
- Проверьте настройки Realtime в Supabase Dashboard

### Проблема: События приходят, но оповещения не показываются

**Проверьте разрешение:**
```javascript
console.log('Notification permission:', Notification.permission);
```

Должно быть `granted`. Если нет - вернитесь к Шагу 1.

**Проверьте, что showNotification вызывается:**
В консоли должны быть логи `[TaskManager] Showing notification for...`

**Попробуйте вручную:**
```javascript
new Notification('Тест', { body: 'Работает?' });
```

Если это работает, а автоматические оповещения нет - возможно проблема в тайминге или async context.

### Проблема: Оповещения приходят только локально

Если оповещения приходят только на том устройстве, где вы совершаете действие, но не на другом:
1. Проверьте, что Realtime события приходят на другом устройстве (смотрите консоль)
2. Если события НЕ приходят - проблема в Realtime подписке
3. Если события приходят, но оповещения нет - проблема в разрешениях на оповещения

## Полезные команды для отладки

### Проверить статус Realtime каналов
```javascript
console.log(supabase.getChannels());
```

### Проверить auth статус
```javascript
const { data: { user } } = await supabase.auth.getUser();
console.log('Current user:', user?.email);
```

### Проверить, что задачи загружаются
```javascript
const { data, error } = await supabase.from('tasks').select('*');
console.log('Tasks:', data, 'Error:', error);
```

### Проверить Realtime подключение вручную
```javascript
const testChannel = supabase
  .channel('manual-test')
  .on('postgres_changes', {
    event: '*',
    schema: 'public',
    table: 'tasks'
  }, (payload) => {
    console.log('MANUAL TEST EVENT:', payload);
    new Notification('Realtime работает!', {
      body: `Событие: ${payload.eventType}`
    });
  })
  .subscribe((status) => {
    console.log('MANUAL TEST STATUS:', status);
  });
```

## Ожидаемое поведение

### При создании задачи
1. Задача сохраняется в БД
2. Realtime отправляет INSERT событие всем подключенным клиентам
3. На всех устройствах:
   - Обновляется список задач (автоматически)
   - Показывается оповещение "Новая задача добавлена"
   - В консоли логи о получении события

### При выполнении задачи
1. Задача обновляется в БД (completed_at)
2. Realtime отправляет UPDATE событие
3. На всех устройствах:
   - Задача визуально помечается выполненной
   - Показывается оповещение "Задача выполнена"
   - В консоли логи о получении события

## Если ничего не помогает

1. Попробуйте перелогиниться
2. Очистите кэш браузера полностью
3. Проверьте, что используете последнюю версию кода (hard refresh: Ctrl+Shift+R)
4. Проверьте консоль на ошибки JavaScript
5. Отправьте мне скриншоты консоли с обоих устройств

## Контрольный список

- [ ] test-notifications.html работает и показывает оповещения
- [ ] В консоли есть лог "Successfully subscribed to Realtime"
- [ ] При создании задачи в консоли появляется "Realtime event received: INSERT"
- [ ] При создании задачи на десктопе событие появляется в консоли мобильного
- [ ] Оповещения разрешены на обоих устройствах (Notification.permission === 'granted')
- [ ] Задачи появляются на другом устройстве без обновления страницы
